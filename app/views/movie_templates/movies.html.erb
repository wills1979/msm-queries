<h1>List of all movies</h1>

<table border="1">
  <tbody>
    <tr>
      <th>
        ID
      </th>

      <th>
        Image
      </th>

      <th>
        Title
      </th>

      <th>
        Year
      </th>

      <th>
        Director
      </th>

      <th>
      </th>
    </tr>
    <% @movie_list.order({:year => :desc, :title => :asc}).each do |a_movie| %>

      <tr>
        <td>
          <%= a_movie.id %>
        </td>
        
        <td>
          <img src=<%= a_movie.image %> alt=<%= a_movie.title %> style="width:250px">
        </td>
        
        <td>
          <%= a_movie.title %>
        </td>
        
        <td>
          <%= a_movie.year %>
        </td>

        <td>
          <%= Director.where({ :id => a_movie.director_id }).at(0).name %>
        </td>

        <td>
          <a href="/movies/<%= a_movie.id %>">Show details</a>
        </td>
      </tr>

    <% end %>
  </tbody>

</table>
